import { useEffect, useState } from 'react'
import { ArrowUpRight, BarChart3, Check, ChevronDown, Circle, Eraser, FileImage, Layers3, LineChart, Menu, MousePointer2, Pencil, Play, Redo2, Ruler, ShieldCheck, Sparkles, Square, Target, Type, Undo2, X } from 'lucide-react'
import './App.css'
import Workspace from './Workspace'

const features = [['01', 'Upload anything', 'Bring in PNG, JPG, WEBP, PDF and screenshots.', FileImage], ['02', 'Paint-style editing', 'If you know Paint, you already know how to use TradeCanvas.', Pencil], ['03', 'Trading drawing tools', 'Trend lines, Fibonacci, channels, rays and risk/reward boxes.', LineChart], ['04', 'Trade planning', 'Make the idea visible: Entry -> Stop Loss -> Target.', Target], ['05', 'Multi-layer workspace', 'Keep your chart, analysis, setup and notes easy to revisit.', Layers3], ['06', 'Undo everything', 'Experiment freely with history, undo and redo.', Undo2]]
const tools = ['Trend Line', 'Horizontal Line', 'Fibonacci', 'Position', 'Risk / Reward', 'Measurement']

function Logo() { return <a className="logo" href="#top" aria-label="TradeCanvas home"><span className="logo-mark"><LineChart size={18} /><Pencil size={10} /></span><span>Trade<span>Canvas</span></span></a> }
function ChartPreview({ compact = false }: { compact?: boolean }) { return <div className={`terminal ${compact ? 'terminal-compact' : ''}`}><div className="terminal-top"><span className="window-dots"><i /><i /><i /></span><span className="terminal-name">TradeCanvas / NIFTY_50_setup.png</span><span className="live"><b /> LIVE PREVIEW</span></div><div className="editor-menu"><span>File</span><span>Edit</span><span>View</span><span>Image</span><span>Trading</span><span>Help</span><span className="editor-spacer" /><span>100%</span><span>⌗</span></div><div className="editor-body"><aside className="tool-rail">{[MousePointer2, Pencil, Eraser, Ruler, Square, Circle, Type, Sparkles].map((Icon, index) => <button className={index === 1 ? 'active' : ''} key={index} aria-label="Drawing tool"><Icon size={15} /></button>)}<div className="rail-rule" />{[LineChart, Target, BarChart3].map((Icon, index) => <button key={index} aria-label="Trading tool"><Icon size={15} /></button>)}</aside><div className="chart-area"><div className="chart-title"><span><strong>NIFTY 50</strong> · 15m</span><em>+1.84%</em></div><div className="price-chart"><div className="zone zone-one" /><div className="zone zone-two" /><div className="fib"><span>2,550</span><i /><span>2,530</span><i /><span>2,510</span><i /><span>2,490</span></div><div className="candles">{[34,46,27,55,40,72,62,82,66,96,72,108,88,122,103,137,121,153,135,170,149,184,166,198].map((height, index) => <span className={index % 4 === 1 ? 'red' : ''} style={{ height: `${height / 2.2}px` }} key={index}><i /></span>)}</div><svg className="trend" viewBox="0 0 600 250" preserveAspectRatio="none"><path d="M20 210 C150 190 210 205 300 145 S450 100 580 30" /><path className="ma" d="M10 180 C120 170 220 168 310 135 S470 110 600 72" /></svg><div className="trade-mark entry">E <small>Entry 2,450</small></div><div className="trade-mark stop">SL <small>2,410</small></div><div className="trade-mark target">TP <small>2,530</small></div><div className="crosshair" /></div><div className="volume">{Array.from({ length: 28 }, (_, i) => <i key={i} style={{ height: `${10 + (i * 13) % 32}px` }} />)}</div><div className="time-axis"><span>09:30</span><span>11:00</span><span>12:30</span><span>14:00</span><span>15:30</span></div></div>{!compact && <aside className="analysis"><div className="analysis-head"><span>Trade analysis</span><span className="green-dot" /></div><div className="setup-badge">BULLISH SETUP <Check size={12} /></div>{[['Entry', '₹ 2,450'], ['Stop loss', '₹ 2,410'], ['Target', '₹ 2,530']].map(([label, value]) => <div className="metric" key={label}><span>{label}</span><strong>{value}</strong></div>)}<div className="risk"><span>Risk / Reward</span><strong>1 : 2.0</strong><div><i /></div></div><div className="mini-note"><span>✦</span> Breakout above resistance<br /><small>Added 2 minutes ago</small></div></aside>}</div><div className="terminal-bottom"><span><Undo2 size={13} /> <Redo2 size={13} /></span><span>Fit to screen</span><span>− &nbsp; 100% &nbsp; +</span></div></div> }

function App() {
  const [menu, setMenu] = useState(false); const [openFaq, setOpenFaq] = useState(0)
  const baseUrl = import.meta.env.BASE_URL
  useEffect(() => {
    document.querySelectorAll<HTMLAnchorElement>('a[href="/workspace"]').forEach((link) => { link.href = `${baseUrl}workspace` })
  }, [baseUrl])
  const faqs = [['Is TradeCanvas a trading platform?', 'No. TradeCanvas is a chart drawing, annotation and analysis workspace. It does not execute trades.'], ['Can I upload screenshots?', 'Yes. Upload chart images you are permitted to use, then annotate them directly in your browser.'], ['Can I draw Fibonacci levels?', 'Yes, the full editor will include trading-specific drawing tools including Fibonacci levels and extensions.'], ['Can I save my analysis?', 'The planned application will support saving and exporting annotated work.'], ['Can I use it on mobile?', 'The interface is responsive, while the full editing experience is optimized primarily for larger screens.']]
  const currentPath = window.location.pathname.replace(/\/$/, '')
  const workspacePath = `${baseUrl.replace(/\/$/, '')}/workspace`
  if (currentPath === workspacePath) return <Workspace />

  return (
    <main id="top"><nav className="nav"><Logo /><div className={`nav-links ${menu ? 'show' : ''}`}><a href="#features" onClick={() => setMenu(false)}>Features</a><a href="#tools" onClick={() => setMenu(false)}>Trading Tools</a><a href="#workflow" onClick={() => setMenu(false)}>How it works</a><a href="#use-cases" onClick={() => setMenu(false)}>Use cases</a><a href="#faq" onClick={() => setMenu(false)}>FAQ</a></div><div className="nav-actions"><a className="login" href="#footer">Log in</a><a className="button button-small" href="/workspace">Launch workspace <ArrowUpRight size={15} /></a></div><button className="menu-button" onClick={() => setMenu(!menu)} aria-label="Toggle navigation">{menu ? <X /> : <Menu />}</button></nav><section className="hero-section wrap"><div className="hero-copy"><p className="eyebrow"><span /> THE VISUAL TRADING WORKSPACE</p><h1>Your trading chart.<br />Your analysis.<br /><em>Your canvas.</em></h1><p className="hero-sub">Upload your charts, screenshots and trading files. Draw directly on them with professional annotation tools built specifically for technical analysis.</p><div className="hero-actions"><a className="button" href="/workspace">Launch TradeCanvas <ArrowUpRight size={17} /></a><a className="text-link" href="#features"><Play size={14} fill="currentColor" /> Explore features</a></div><p className="microcopy"><span>No complicated setup</span><i /> <span>Browser-based</span><i /> <span>Built for traders</span></p></div><div className="hero-art"><div className="art-glow" /><ChartPreview /></div></section><section className="proof-strip"><div className="wrap proof-inner"><p>Everything you need to analyze a setup</p>{[['Draw', Pencil], ['Annotate', Sparkles], ['Measure', Ruler], ['Plan', Target], ['Save', ShieldCheck]].map(([name, Icon]) => <div className="proof-item" key={name as string}><Icon size={16} /><span><strong>{name as string}</strong><small>{name === 'Draw' ? 'Precision tools' : name === 'Annotate' ? 'Mark key levels' : name === 'Measure' ? 'Calculate movement' : name === 'Plan' ? 'Visualize the trade' : 'Stay organized'}</small></span></div>)}</div></section><section id="features" className="section wrap"><div className="section-heading"><p className="eyebrow">01 / THE TOOLKIT</p><h2>More than Paint.<br /><em>Built for trading.</em></h2><p>A clear, capable canvas for the moments when a screenshot needs to become a decision.</p></div><div className="feature-grid">{features.map(([number, title, text, Icon]) => <article className="feature-card" key={title as string}><span className="card-number">{number as string}</span><Icon size={21} /><h3>{title as string}</h3><p>{text as string}</p><ArrowUpRight className="card-arrow" size={17} /></article>)}</div></section><section id="workflow" className="dark-band"><div className="wrap workflow"><div className="section-heading"><p className="eyebrow">02 / THE FLOW</p><h2>From screenshot<br /><em>to setup.</em></h2></div><div className="steps">{[['01', 'Upload', 'Drop in your chart or screenshot.'], ['02', 'Analyze', 'Draw the levels that matter.'], ['03', 'Plan', 'Save your completed trade idea.']].map(([num, title, text], i) => <div className="step" key={num}><span className="step-num">{num}</span><div className="step-line" /><h3>{title}</h3><p>{text}</p>{i < 2 && <ArrowUpRight size={18} />}</div>)}</div></div></section><section id="tools" className="section wrap"><div className="section-heading centered"><p className="eyebrow">03 / TRADING TOOLS</p><h2>Designed around the way<br /><em>traders think.</em></h2></div><div className="tools-layout"><div className="tool-list">{tools.map((tool, index) => <button className={index === 0 ? 'selected' : ''} key={tool}><span>0{index + 1}</span>{tool}<ArrowUpRight size={15} /></button>)}</div><div className="tool-demo"><ChartPreview compact /><div className="demo-label"><span /><strong>Trend line</strong><small>Mark momentum at a glance</small></div></div></div></section><section className="before-after"><div className="wrap"><div className="section-heading"><p className="eyebrow">04 / MAKE IT MEAN SOMETHING</p><h2>Turn a chart into<br /><em>a trading plan.</em></h2></div><div className="comparison"><div className="compare-pane before"><span>BEFORE</span><div className="bare-chart"><LineChart size={36} /><p>Uploaded chart</p><small>No annotations yet</small></div></div><div className="slider-pill">↔</div><div className="compare-pane after"><span>AFTER</span><div className="annotated"><div className="fake-line" /><div className="fake-zone" /><b>ENTRY</b><strong>2,450</strong><i>SL 2,410</i><em>TP 2,530</em><p>Breakout + retest</p></div></div></div></div></section><section id="use-cases" className="section wrap"><div className="section-heading"><p className="eyebrow">05 / BUILT FOR THE WHOLE PROCESS</p><h2>One canvas.<br /><em>Infinite analysis.</em></h2></div><div className="use-grid">{[['Technical analysis', 'Mark market structure and important price levels.', LineChart], ['Trade planning', 'Visualize entries, stops and targets before the trade.', Target], ['Trade journaling', 'Save annotated screenshots to review past trades.', FileImage], ['Education', 'Explain setups with clarity in the classroom or community.', Sparkles]].map(([title, text, Icon]) => <article key={title as string}><Icon size={22} /><h3>{title as string}</h3><p>{text as string}</p><ArrowUpRight size={17} /></article>)}</div></section><section className="workspace-band"><div className="wrap workspace-content"><div className="section-heading"><p className="eyebrow">06 / THE NEXT PHASE</p><h2>Your future<br /><em>workspace.</em></h2><p>The full browser-based editor is coming next. A familiar canvas, with the tools your analysis actually needs.</p><a className="button" href="/workspace">Open workspace preview <ArrowUpRight size={16} /></a></div><ChartPreview /></div></section><section className="comparison-section section wrap"><div className="section-heading centered"><p className="eyebrow">07 / WHY TRADECANVAS</p><h2>Generic tools draw.<br /><em>TradeCanvas understands.</em></h2></div><div className="compare-table"><div><h3>Traditional screenshot editing</h3>{['Generic drawing tools', 'No trading tools', 'Difficult trade planning', 'No risk / reward view', 'Not designed for analysis'].map(x => <p key={x}><X size={15} />{x}</p>)}</div><div className="trade-side"><h3>TradeCanvas</h3>{['Trading-focused tools', 'Upload charts instantly', 'Entry / SL / Target', 'Fibonacci + risk / reward', 'Layers, history, export'].map(x => <p key={x}><Check size={15} />{x}</p>)}</div></div></section><section className="privacy-band"><div className="wrap privacy"><ShieldCheck size={27} /><div><p className="eyebrow">08 / YOUR DATA</p><h2>Your charts. Your analysis. <em>Your data.</em></h2><p>Secure file handling, privacy-focused workspace and a workflow that keeps your uploaded files under your control.</p></div></div></section><section id="faq" className="section wrap faq"><div className="section-heading"><p className="eyebrow">09 / QUESTIONS</p><h2>Good to know.</h2></div><div className="faq-list">{faqs.map(([q, a], i) => <div className={`faq-item ${openFaq === i ? 'open' : ''}`} key={q}><button onClick={() => setOpenFaq(openFaq === i ? -1 : i)}><span>{q}</span><ChevronDown size={18} /></button>{openFaq === i && <p>{a}</p>}</div>)}</div></section><section className="final-cta"><div className="cta-chart" /><div className="wrap"><p className="eyebrow">THE NEXT MOVE IS YOURS</p><h2>Stop drawing around your trades.<br /><em>Start trading with a canvas.</em></h2><p>Upload. Analyze. Annotate. Plan.</p><a className="button" href="/workspace">Launch TradeCanvas <ArrowUpRight size={17} /></a></div></section><footer id="footer" className="footer wrap"><div><Logo /><p>Draw your trade. See the setup.</p></div><div className="footer-links"><div><strong>Product</strong><a href="#features">Features</a><a href="#tools">Trading tools</a><a href="#workflow">Workspace</a></div><div><strong>Resources</strong><a href="#faq">FAQ</a><a href="#workflow">Tutorials</a><a href="#footer">Roadmap</a></div><div><strong>Company</strong><a href="#footer">About</a><a href="#footer">Contact</a><a href="#footer">Privacy</a></div></div><div className="footer-bottom"><span>© 2026 TradeCanvas. All rights reserved.</span><small>TradeCanvas is an analysis and visualization tool. It does not provide financial advice or execute trades.</small></div></footer></main>
  )
}

export default App
